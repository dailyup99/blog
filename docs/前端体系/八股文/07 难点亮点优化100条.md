---
outline: deep
---

## Webapck难点亮点

## 1.优化：入口文件优化，提取公共的包

![image-20250111034602381](http://139.196.79.103:9001/myimages/imgs/20250111034609504.png)

比如，有组件A和组件B，它们都使用到了util工具方法C，它的大小有20K，假如没有进行分包处理，那么最终打包出来的js文件大小会多出20K，因为它被打包了2次；如果将这个公共的util工具方法C抽取成公共的文件，那么最终打包出来的js文件大小就可以节省20K的大小。

## 2.难点亮点：splitChunks配置文件优化

```javascript
module.exports = {
    opimization: {
        splitChunks: {
            chunks: 'all', // 对所有类型的块进行分割
            minSize: 20000, // 最小块大小，单位字节
            maxSize: 0, // 最大块大小，0表示不限制
            minChunks: 1, // 被引用的最小次数
            name: true, // 使用自动命名
            cacheGroups: {
                vendor: {
                    test: /[\\/]node_modules[\\/]/, //匹配node_modules中的模块
                    name:'vendors', // 生成的块名称
                    chunks: 'all'
                },
                default: {
                    minChunks: 2, // 最小引用次数
                    priority: -20, // 优先级
                    reuseExistingChunk: true, // 重用已存在的模块
                }
            }
        }
    }
}
```

minSize拆分包的大小，至少为minSize，如果一个包拆分出来达不到minSize，那么这个包就不会拆分。

minChunks是被引用的最小次数，就是上面的util工具C被引用的次数只要超过1就会分包处理

## 3.优化：cacheGroups缓存优化

可以对打包出来的这个公共vendor.js文件做一个长期缓存

组件A和组件B它们是属于业务，打包出来不会缓存，会生成hash，比如a.dadasda.js，b.kdkadkalkd.js

公共部分的代码一般不会怎么变化，可以进行缓存，vendor也可以加hash，vendor.[hash].js

## 4.优化：CDN内容分发

CDN的优化点在于，我资源首次加载没有命中缓存，需要去下载js、CSS等，才会访问CDN。

1.CDN的好处：降低服务器的压力，将压力平摊，用多台服务器顶住压力

2.用户会寻找最近的CDN服务器，减少网络延迟

业务资源和vendor资源，都可以通过CDN进行分发，提升性能

## 5.优化：浏览器缓存策略优化

在我们的项目中，大部分情况是直接使用强缓存。

协商缓存和强缓存是浏览器缓存机制中的两个重要概念，主要用于提高网页加载速度和减少服务器负担。

**强缓存**

强缓存是指在浏览器中直接使用缓存的资源，而不需要向服务器发送请求。强缓存的实现主要依赖于HTTP响应头中的`Cache-Control`和`Expires`。

* Cache-Control：这个头部可以设置缓存的策略，例如：
	* `max-age=3600`：表示资源可以被缓存3600秒（1小时）
	* `no-cache`：表示强制协商缓存，即每次都要向服务器验证
	* `public`或`private`：指定资源是否可以被共享缓存
* Expires：这个头部指定了资源的过期时间，格式为HTTP日期。过期后，浏览器回想服务器请求新的资源

**协商缓存**

协商缓存是指在强缓存过期后，浏览器向服务器发送请求，询问资源是否被修改。协商缓存的实现主要依赖于HTTP请求头中的`If-None-Match`和`If-Modified-Since`。

* Etag: 服务器在响应中返回的一个唯一标识符，表示资源的版本。浏览器在后续请求中会将这个标识符放在`If-None-Match`请求头中，服务器会根据这个标识符判断资源是否被修改。
* Last-Modified：服务器在响应中返回的资源最后修改时间。浏览器在后续请求中会将这个时间放在`If-Modified-Since`请求头中，服务器会根据这个时间判断资源是否被修改。

**总结**

* 强缓存：直接使用缓存，不向服务器请求，依赖于`Cache-Control`和`Expires`
* 协商缓存：在强缓存过期后，向服务器请求验证资源是否被修改，依赖于`Etag`和`Last-Modified`

这两种缓存机制可以有效地较少网络请求，提高网页加载速度。

## 6.优化：GZIP资源压缩优化

Js格式文件=>GZIP格式

1.文本文件：对于纯文本文件（如HTML、CSS、JavaScript）

2.图像文件：对于图像文件（如JPEG、PNG），GZIP的效果通常不明显，因为这些格式已经经过压缩

3.其他类型文件：对于某些类型的文件（如XML、JSON、SCV），GZIP也能提供良好的压缩效果

前端打包出来给后台的是JS、CSS的元素文件。

GZIP一般是CDN去做，CDN去配置，自动进行GZIP压缩。

流程大概如下：

前端=>上传aaa.js=>服务器=>上传CDN=>CDN将js转成GZIP。

用户访问=>CDN=>返回aaa.zip=>浏览器解压=>aaa.js

## 7.极致优化: babel插件包体积优化

```javascript
module.exports = {
    presets: [
        '@babel/preset-env',
        {
            targets: {
                browsers: ["> 0.25%", "not dead"]
            }
        }
    ],
    plugins: [
        '@babel/plugin-transform-runtime'
    ]
}
```

我们会使用polyfill将ES6的代码转为ES5，在安装babel依赖的时候有个babel-core或者有的地方叫corejs，假如有两个文件a.js和b.js都用到了Object.assign方法

a.js=>a.xdadad.js (corejs assign)

b.js => b.jdjakdla.js (corejs assign)

@babel/plugin-transform-runtime会找到我们项目中通用的corejs中的方法，放在公共部分。

## 8.hash粒度控制缓存优化

1.hash

2.chunkhash 1% （基于整个chunk ,可以理解为一个组件分包）

3.contenthash 99% （基于文件 粒度最细致）

比如我们现在有两个入口index.js和main.js，它们打包之后分别会输出到不同的bundle文件中，并且文件名称使用了hash；假如修改了index.js文件中的内容，那么hash就会发生改变，也就意味着两个文件的名称都发生了变化。

chunkhash 可以有效解决上面的问题，它会根据不同的入口来解析生成的hash值：

修改了index.js，main.js中的chunkhash 是不会发生改变

contenthash 表示生成的文件hash名称只和内容有关：

比如index.js中引入一个style.css，style.css被抽取到一个独立的css文件，这个css文件如果使用chunkhash，那么当index.js文件中的内容发生改变时，css文件的命名也会发生变化，那我们就可以使用contenthash 。

## 9.将CSS进行单独抽离

js的内容变化，不会影响css文件，也不会影响css的hash，css可以很好的利用缓存

```javascript
// webpack.config.js
const path = require('path');
const MiniCssExtractPlugin = require('mini-css-extract-plugin');

module.exports = {
    entry: './src/index.js',// 入口文件
    output: {
        filename: 'bundle.js',
        path:path.resolve(__dirname, 'dist'),
    },
    module: {
        rules: [
            {
                test:'/\.css$/', // 匹配CSS文件
                use: [
                    MiniCssExtractPlugin.loader, // 提取CSS
                    'css-loader', // 处理CSS文件
                ]
            }
        ]
    },
    plugins: [
        new MiniCssExtractPlugin({
            filename: '[name].css', // 输出的CSS文件名
            chunkFilename: '[id].css', // 处理chunk的文件名
        })
    ]
}
```

## 10.难点和亮点：sourcemap调试优化

为webapck中，sourcemap是一种用于调试的工具，它可以将编译后的底阿妈映射回原始源代码。不同的sourcemap配置选项会影响生成的sourcemap的质量、大小和性能。以下是一些常见的sourcemap配置选项及其区别：

1.`source-map`

* 描述：生成完整的source map文件，适合生成环境
* 优点：提供完整的映射，便于调试
* 缺点：构建速度慢，生成的文件较大

2.`inline-source-map`

* 描述：将sourcemap作为Data URL嵌入到生成的文件中
* 优点：方便调试，无需额外的请求来获取sourcemap
* 缺点：生成的文件体积较大，适合开发环境

3.`eval-source-map`

* 描述：使用`eval()`包裹每个模块的代码，并生成sourcemap
* 优点：构建速度快，适合开发环境
* 缺点：生成的sourcemap不够完整，可能会影响性能

4.`cheap-source-map`

* 描述：生成较小的sourcemap，不包含列映射
* 优点：构建速度快，适合开发环境
* 缺点：不支持列映射，调试时可能不够精确

5.`cheap-module-source-map`

* 描述：与`cheap-source-map`类似，但支持对loader的sourcemap进行映射
* 优点：适合开发环境，构建速度快
* 缺点：同样不支持列映射

6.`hidden-source-map`

* 描述：生成sourcemap，但不在浏览器中暴露
* **优点：适合生产环境，可以在调试时使用，但不暴露给用户**
* 缺点：调试时需要手动加载sourcemap

7.`nosources-source-map`

* 描述：生成sourcemap，但不包含源代码
* 优点：适合生产环境，保护源代码
* 缺点：调试时无法查看源代码

**总结：**

选择合适的sourcemap配置取决于您的开发和生产需求。通常在开发环境中使用`eval-source-map`或`inline-source-map`，而在生产环境中使用`source-map`或`hidden-source-map`。根据项目的需求和性能考虑，您可以选择最适合的配置。

## 11.优化：监测webpack编译速度和性能

性能优化的前提：我得先知道哪里慢？不是凭感觉。

```javascript
// 性能监控
const SpeedMeasurePlugin = reuqire("speed-measure-webpack-plugin");
const smp = new SpeedMeasurePlugin();

// Generate configuration
const config = smp.wrap(configFactory('production'));
```

知道speed-measure-webpack-plugin这个名字，然后可以看到每个插件耗费多少时间，黄色的表示耗费时间比较长，可以进行优化。

![image-20250111135809432](http://139.196.79.103:9001/myimages/imgs/20250111135816519.png)

## 12.难点亮点：优化js压缩混淆速度terserPlugin

从**7s**降低到**1.5s**，它的作用主要有两个：

1.开启多线程任务并行

2.AST抽象语法树进行了优化和复用

```javascript
// webpack.config.js
const path = require('path');
const TersetPlugin = require('terser-webpack-plugin');

module.exports = {
    mode: 'production', // 设置为生产模式
    entry: './src/index.js', // 入口文件
    output: {
        filename: 'bundle.js', // 输出文件名
        path: path.resolve(__dirname, 'dist'), // 输出路径
    },
    optimization: {
        minize: true, // 启用压缩
        minimizer: [
            new TerserPlugin({
                terserOptions: {
                    compress: {
                        drop_console: true, // 删除console.log
                    },
                    output: {
                        comments: false, // 删除注释
                    }
                },
                extractComments: false, // 不提取注释到单独文件
             })
        ]
    }
}
```

## 13.优化：SWC提升js、ts的编译性能

SWC之所以快，主要有以下几个原因：

1.Rust编写：SWC是Rust编写的，Rust是一种高性能的系统编程语言，具有优秀的内存管理和并发性能。这使得SWC在执行速度上优于许多用js或其他语言编写的编译器。

2.并行处理：SWC支持多行并行处理多个文件，这意味着可以同时编译多个模块，从而显著提高构建速度，尤其是在大型项目中。

3.高效的AST（抽象语法树）处理：SWC在编译过程中使用高效的AST处理，能够快速分析和转换代码结构，减少不必要的操作。

4.优化的编译流程：SWC采用了优化的编译流程，能够快速进行语法分析、转换和生成代码，减少了编译过程中的开销。

5.增量编译：SWC支持增量编译，只编译发生变化的文件，而不是每次都重新编译整个项目，这样可以大幅度减少构建时间。

6.现代特性支持：SWC支持现代js和ts特性，能够高效处理新语法，避免了对旧特性的兼容性处理，从而提高了编译速度。

通过这些设计和实现，SWC能够在保持高效性能的同时，提供快速的编译体验，适用于现代前端开发中的构建工具。

![image-20250111142156768](http://139.196.79.103:9001/myimages/imgs/20250111142156813.png)

**性能相比babel提升了几倍到10倍**

用speed-measure-webpack-plugin这个插件分析，它可以查看构建速度，自己跑项目看提升了几倍。

## 14.难点解决思维：分析包体积，查找项目瓶颈

```javascript
const BundleAnalyzerPlugin = require('webpack-bundle-analyzer').BundleAnalyzerPlugin;
```

![image-20250111143008061](http://139.196.79.103:9001/myimages/imgs/20250111143008100.png)

**包体积的优化思路：找主要矛盾。**

也就是找到包体积比较大的部分对它进行优化。

## 15.优化：webpack5的原生runtime chunk优化

```javascript
optimization: {
    runtimeChunk: {
        name: 'runtime'
    }
}
```

runtime.js是webpack的运行上下文。

面试吹：10k => 5k

原始包体积

![image-20250111143515886](http://139.196.79.103:9001/myimages/imgs/20250111143515929.png)

优化后：

![image-20250111143530534](http://139.196.79.103:9001/myimages/imgs/20250111143530569.png)

## 16.优化：将npm的依赖打包成公共的vendor

![image-20250111143930564](http://139.196.79.103:9001/myimages/imgs/20250111143930598.png)

cacheGroups是对npm中的包做缓存，因为npm中的包是几乎不变的。

注意和入口文件优化，打包成公共的包的区别，入口文件优化，打包成公共的包是业务代码打包成公共包，而这个是将npm依赖打包成公共的包。

## 17.难点亮点：对vendor打包的进一步优化

vendor还可以继续分类。

按需加载的vendor1分一个类型（比如antd）

全量打包的vendor2划分一个类型（比如react redux）

如果没有对vendor进行拆分，都是打包到同一个vendor里面，那么当按需引入的antd发生改变时，整个vendor就会重新打包，但是实际上react的包并没有发生变化，不需要重新打包。

## 18.极致优化：reuseExistingChunk复用已存在的chunk

对npm的依赖，有使用到的相同代码进一步复用。

遇到了什么问题：npm的依赖可能有相同的代码

解决思路：公共部分抽离复用

怎么解决：reuseExistingChunk配置

## 19.优化：webpack5带来的编译优化

webpack首次打包都很慢，从头编译。

利用缓存。

webpack5新版本默认是带缓存的。在node_modules目录下有个.cache文件会将一些ast或者一些编译信息缓存起来，下次编译直接使用

![image-20250111174729600](http://139.196.79.103:9001/myimages/imgs/20250111174729632.png)

编译时间：从60s=>1s

我们一起是基于webpack4，webpack4没有这个特性，所以编译很慢。

webpack4可以利用插件进行缓存，但是不是原生的。性能不如webpack5好。

## 20.优化：Tree shaking树摇，删除多余代码

1.删除没用到的模块

2.要用es6 module

追问？

你的webpack怎么配置tree shaking?

* useExports
* sideEffects

sideEffect是在package.json中配置的，就是手动告诉webpack哪些模块肯定是es6，你放心的删除没用到的模块。如果没有配置sideEffects，可能有些代码删不掉。

我来之前使用useExports，我来之后使用sideEffects，更有效地跳过整个模块和整个文件子树。

## 21.ts编译存在瓶颈，使用多线程来优化

ts-loader

使用ForkCheckerWebpackPlugin插件来开启ts的多线程编译

![image-20250111180354130](http://139.196.79.103:9001/myimages/imgs/20250111180354177.png)

性能优化是具有辩证性的。

不是所有的优化一定有正面效果，你得到了什么，一定会损失一些东西。

**优化的时候，要结合speed插件，去观测优化的效果。**

ts => 1s

多线程=>2s

这种情况下因为开启多线程反而打包时间更长，就没有必要使用ForkCheckerWebpackPlugin

开启多线程本身就具有性能损耗。

ts => 50s

多线程 => 15s

## 22.难点：如何选择哪些资源可以开启多线程优化性能

比如：js、ts、代码的混淆(terser)输出

比如CSS的编译，less-loader=>css-loader=>style-loader，互相有依赖不能开启多线程的优化

图片处理：可能可以 图片A => 图片A1

## 23.难点亮点：ts类型检查优化（非常推荐说这个）

项目webpack编译过程

1.ts类型检查

2.对ts进行编译

很多项目，类型检查实际上是很消耗性能



为什么不可以进行类型检查？

因为我们本地的编辑器插件可以进行检查。

在使用ts-loader时，可以设置transpileOnly: true，这样ts只会进行转译，而不进行类型检查。类型检查可以通过ForkTsCheckerWebpackPlugin异步进行。

![image-20250111182104233](http://139.196.79.103:9001/myimages/imgs/20250111182104268.png)

使用ForkTsCheckerWebpackPlugin，对ts的类型检查在单独的进程进行。

![image-20250111182141119](http://139.196.79.103:9001/myimages/imgs/20250111182141147.png)

优化tsconfig.json:

* slitLibCheck: 设置为true，可以跳过库文件的类型检查
* noEmit：开发模式下可以设置为true，避免生成输出文件
* incremental：启用增量编译，ts会缓存上次编译的结果，只会编译改动的代码

![image-20250111182433472](http://139.196.79.103:9001/myimages/imgs/20250111182433500.png)

减少类型检查的范围：

* 确保只在需要的文件夹中进行类型检查，避免不必要的文件
* 使用include和exclude选项来限制检查的文件范围

## 24.优化：组件分包到底解决的是什么问题？

```javascript
const module = await import('./CpA');
```

最终打包的产物CpA.djajkda.js

假如项目有5个页面 A B C D E，它们的大小都是100K

不分包，加载index.dadadsa.js 大小是这五个页面的总和500K

首屏加载500K的js就很慢

如果我们进行分包

A.xcdada.js 100K

B.djkakdja.js 100K

...

我们访问首页的时候只需要加载100K的js，速度一下就提升了，然后再访问其他页面，比如B页面，再去异步加载B组件对应的100K的js文件。

## 25.难点亮点：webpack中loader的配置优化

反面教材：

将所有的js，包括node_modules中的文件内容都进行babel编译

![image-20250111183434702](http://139.196.79.103:9001/myimages/imgs/20250111183434733.png)

正面教材：

通过配置include，只对src目录下的代码进行babel编译

![image-20250111183452502](http://139.196.79.103:9001/myimages/imgs/20250111183452534.png)

## 26.极致难点：极致优化webpack的模块解析时间

```javascript
resolve: ['js', 'jsx', 'ts', 'tsx']
```

```javascript
import A from '/src/a'
```

这种方式会从src下面去寻找所有的文件，找到a，然后找到后缀是否有['js','jsx','ts','tsx']中的一个，最终找到a.js

推荐尽量准确引用依赖

```javascript
import A from 'src/a/js'
```

## 27.难点亮点：DLL编译优化

将通用的，经常不变化的库，抽取成一个共享的库。比如vue、vuex

webpack.config.js（业务代码）

webpack.dll.js（第三方库）

会打包生成一个vendor.dll.js

![image-20250111185248267](http://139.196.79.103:9001/myimages/imgs/20250111185248310.png)

package.json

```javascript
{
  "scripts": {
    "dll": "webpack --config ./webpack.dll.js"
  }
}
```

dll和前面的vendor有什么区别？

dll只编译一次，后面没有变化就不用再编译。

## 28.js编译开启多线程

![image-20250111185553166](http://139.196.79.103:9001/myimages/imgs/20250111185553208.png)

开多线程，一定要谨慎，要配合speed插件，看是否真的提升了。

开多线程，自身消耗也不小。

## 29.细节优化：删除webpack的进度条显示

微乎其微的提升。

我们在npm run dev或npm run build的时候有个进度条显示，

将`ProgressPlugin`，从webpack中删除可以缩短构建时间。请注意`ProgressPlugin`可能不会为构建速度提供太多价值，因此请权衡利弊再使用。

## 30.优化：在webpack中避免在开发环境使用生产环境才需要的内容

* `TerserPlugin` 代码压缩
* `[chunkhash]/[contenthash]`

## Vite难点亮点

## 31.优化：我为什么选择vite?（技术选型）

vite分为两个核心阶段

1.开发环境编译 ESbuild

2.生产环境打包Rollup

ESbuild是Go实现的，更贴近底层，性能会更好，AST的复用、并发、多线程。

利用了原生的es6 module，是真正的按需加载。

但是webpack需要分析项目的整个依赖

![image-20250111194941369](http://139.196.79.103:9001/myimages/imgs/20250111194941403.png)

那为什么打包用Rollup呢？

esbulid专注解决的是ts js的编译，但是还有很多其他资源 css、图片、路径等等，需要各种插件。借助了rollup的能力。

使用esbuild+rollup扬长避短，携手并进。

配置比webpack相对要简单，上手更快，而且性能不错，不用主动去做很多优化，它能保证一个性能的底线。

## 32.难点亮点：打包UMD模块

你的项目用的什么模块化方案？

* es6 module
* commonjs

一般都是es module

你的项目打包出来的文件遵循什么规范？

UMD的规范。它是为了解决js在不同环境中模块化的问题。

通用性非常强，一套规范，几乎所有的项目都可以使用。

大概类似下面的代码：

![image-20250111200037589](http://139.196.79.103:9001/myimages/imgs/20250111200037618.png)

## 33.亮点：Vite在开发环境下不进行类型检查

巨大的性能提升。

esbuild是按需引用。

ts的类型检查要分析全局的依赖。

两种是冲突的。



借助编辑器的ts类型检查，和vite互不冲突。

## 34.难点亮点：css编译性能优化

yy一个场景：我们css编译我发现非常慢，于是我提升了它的性能。

`https://lightingcss.dev/`

![image-20250111200714361](http://139.196.79.103:9001/myimages/imgs/20250111200714397.png)

原先打包css需要用8s，打包之后只需要80ms

## 35.亮点：项目别名配置

Webpack和Vite通用

![image-20250111201011833](http://139.196.79.103:9001/myimages/imgs/20250111201011873.png)

## 36.优化：预构建优化性能解决流量瀑布问题

vite有一个非常经典的问题。

按需加载。

假如引入了一个js，而这个js又引入很多其他js

index.js

```javascript
import xxx from 'lodash'
import xxx from 'dayjs'
```

项目初始化，加载上千个js

可能会导致，开发环境会加载上千个js，很慢。



预构建解决2个问题

* 分析依赖，将这些多个依赖合并打包在一起加载
* 有些第三库比较老，不支持es6 module，把不支持es6 module的转成支持

## 37.优化：使用预构建，优化lodash-es开发环境加载性能并且利用缓存

```javascript
optimizeDeps: {
    exclude: ['lodash-es']
}
```

非常典型的lodash-es包引发的问题

lodash的每个引入的方法都会打包成一个个的js

![image-20250111202249363](http://139.196.79.103:9001/myimages/imgs/20250111202249417.png)

开启预构建之后，就会打包成一个独立的lodash-es.js

![image-20250111202322397](http://139.196.79.103:9001/myimages/imgs/20250111202322444.png)

预构建的缓存，它是缓存在.vite/deps中，下次编译的时候就可以使用缓存的内容

![image-20250111202354687](http://139.196.79.103:9001/myimages/imgs/20250111202354734.png)

## 38.难点亮点：使用Vite非常巧妙兼容低版本浏览器

![image-20250111202957043](http://139.196.79.103:9001/myimages/imgs/20250111202957088.png)

这里主要使用到legacy，有些客户使用低版本浏览器，需要兼容适配

![image-20250111203025855](http://139.196.79.103:9001/myimages/imgs/20250111203025889.png)

打包出来会有2个js文件一个是index-lefacy-xxx.js和polyfills-legacy-xxx.js

polyfills是用来补充低版本没有的方法。

![image-20250111203220055](http://139.196.79.103:9001/myimages/imgs/20250111203220105.png)

浏览器在加载时会同时引入module和nomodule，module对应的是index-lefacy-xxx.js，而nomodule对应的是polyfills-legacy-xxx.js，那么在高版本浏览器就会加载module，低版本浏览器加载nomodule。

## 39.优化：vite双引擎架构生产环境100%用rollup吗？

不是。

比如生产环境js的编译、js的压缩用的是es build，借助它的性能非常好。

rollup有繁荣的生态，其他资源的处理css、图片、json等等。

开发环境以esbuild为主，生产环境以rollup为主。

## 40.优化：vite减少路径解析优化编译性能

不管webpack和vite都可以这样优化

![image-20250111204142173](http://139.196.79.103:9001/myimages/imgs/20250111204142211.png)

## 41.优化：vite避免导入桶文件，明确到具体的方法

![image-20250111204417167](http://139.196.79.103:9001/myimages/imgs/20250111204417197.png)

存在问题

* 全部加载尽量，增大构建的体积
* 会运行引入的js，可能会包含影响项目的代码

建议

```javascript
import { slash } from './utils/slash.js'
```

## 42.难点亮点：一些vite配置常见的优化建议

使用vite，尽量避免使用babel，借助esbuild。

保持vite如此之快的关键在于减少源文件(js/ts/css)的工作量。

精简工作的例子：

* 使用css而不是sass/less/stylus（可以由postcss处理嵌套）
* 不要使用`@vitejs/plugin-react-refresh`，而是使用React Fast Refresh的原生支持
* 当使用@vitejs/plugin-react时，避免配置Babel选项，这样它就会在构建期间跳过转换（只使用esbuild）

使用更原生化工具链的例子：

使用更原生化的工具链往往会带来更大的安装大小，因此在启动新的vite项目时不是默认的。但对于较大的应用程序来说，这可能是值得的。

* 尝试实验性的LightingCSS
* 使用@vitejs/plugin-react-swc代替@vitejs/plugin-react

## 业务架构优化

## 43.优化：请求统一到request.js文件

![image-20250111220907654](http://139.196.79.103:9001/myimages/imgs/20250111220907697.png)

大家经常这么写

```javascript
mounted() {
    axios.get('/api/xxx')
}
```

缺点：一个请求可能多个地方使用。

假如后台改了接口或者参数，全跟着改，很容易改错或者改漏。

正确写法：

```javascript
import { logoutReuquest} from 'util'

mounted() {
    logoutRequest(params)
}
```

## 44.优化：ts的项目尽量不要出现any

出现any失去了ts的意义

谁写了any,请大家喝奶茶

必须要写any,要报备

通过软实力，提高代码质量

体现你的技术之外的能力、责任心、管理能力

用第三方库的时候，使用第三方库提供的类型

下载

```javascript
npm i @types/react
```

ts的类型 输入输出都写

## 45.优化：业务组件目录结构设计优化

不好：

![image-20250111221949322](http://139.196.79.103:9001/myimages/imgs/20250111221949358.png)

好：

![image-20250111222000200](http://139.196.79.103:9001/myimages/imgs/20250111222000239.png)

## 46.优化：项目整体目录结构设计优化

宏观优化

![image-20250111222314624](http://139.196.79.103:9001/myimages/imgs/20250111222314676.png)

## 47.优化：项目通用优化细节

1.没有用到的变量全部删除

2.开启prettier或者eslint辅助我们代码规范统一

* 插件
* 缩进
* Eslint

3.尽量使用alias引入项目依赖'@/xxx'

* 复制代码的时候，不用管项目的层级

4.无用的注释删掉

* 不要写一大堆注释，方法名就是最好的注释

5.删除无用的console.log

## 48.优化：函数设计优化，尽量见到函数名就知道做了什么

```javascript
function getUserInfo(params: Params): ResData {
    ...
}
```

函数：

1.做什么？

2.参数是什么？

3.返回什么？

函数一定要原子化，功能要纯粹，getUserInfo只做获取用户信息的事情，其他一律不要做。不要获取用户信息然后又去检测登录状态，如果要检测登录状态，那么就再写一个方法。

```javascript
function checkLogin() {}
```

面试可以结合code review去讲，你是如何审核别人代码。

## 49.难点亮点：网络错误处理，以及code设计

通用的是通过axios中间件，在响应拦截器里面处理

![image-20250113153315497](http://139.196.79.103:9001/myimages/imgs/20250113153322603.png)

![image-20250113153326732](http://139.196.79.103:9001/myimages/imgs/20250113153326773.png)

```javascript
{
    data: {
        user: 'xxx'
    },
    code: 0,
    msg: 'ok'
}
```

code是业务状态码。

一般0代表成功，-1代表失败。msg表示错误信息。

和http status有什么区别？

301 304 401 404

有缺陷，http，0-500多。只有500多种。

业务状态码，code爱给什么给什么，有很多种。

## 50.难点亮点：项目全局错误处理

React

![image-20250113153920564](http://139.196.79.103:9001/myimages/imgs/20250113153920618.png)

![image-20250113153935252](http://139.196.79.103:9001/myimages/imgs/20250113153935289.png)

Vue3

![image-20250113154005338](http://139.196.79.103:9001/myimages/imgs/20250113154005376.png)

## 51.优化：组件内部局部错误处理

局部错误处理怎么做？

一般只做全局的就OK。

比如：我有个组件的错误，希望单独处理。

```javascript
class App extends React {
    componentDidMount() {
        try {
            // 特别复杂的业务逻辑
        } catch(err) {
            // 记录err 输出本地日志
            // 上报err 传给后台post请求
        }
    }
}
```

## 52.优化：组件业务代码组织设计优化（代码规范）

1.符合代码规范eslint tslint 缩进 结尾分号、逗号；

2.业务设计

* 第三方的库、自定义的钩子
* 取数据，比如从store中取
* 生命周期的钩子
* 业务函数

## 53.优化：页面警告与报错处理

1.开发环境，处理所有的警告信息warning error

2.生产环境，上线之后观察有没有警告或者error

好处：

* 依赖的版本落后了
* 框架会给你一些性能提示
* 业务有错误，但是刚好不影响主流程运行，但是隐藏的很深
* 可能造成安全问题

**你觉得你跟组内其他人有什么不一样的地方？**

可以说这个。

你不一定是组内技术最好的人，才受领导器重，但是一定要是最负责的那个人。

## 54.难点亮点：组件渲染次数性能检测

自定义的钩子实现，组件的渲染次数监测

1.监测更新的render次数

2.每次更新花费的时间

![image-20250113160759505](http://139.196.79.103:9001/myimages/imgs/20250113160759544.png)

## 55.优化：关于注释和文档的处理建议

1.代码就是最好的注释。

* 如果我看不懂你写的代码？
  * 业务真的很复杂（要写注释）
  * 业务不复杂（我看到方法名，就知道代码是什么意思，不要注释）
    * 如果看不懂，有可能是没有遵循原子性的原则（就是一个函数只做一件事情）

2.项目的介绍

* 环境安装 启动项目 打包
* 项目目录结构介绍，参考项目架构设计内容
* 接口文档的地址、联调的配置

## 生产效率优化

组长

项目负责人

项目核心 高级

需要关注的事情

**核心：让大家更好的出活**

## 56.优化：数据状态调试优化

利用redux-devtool查看项目数据状态

1.可以时间旅行，查看每一条状态对UI的影响

* 查bug非常方便

2.查看什么数据引起了渲染

3.项目全部都是redux数据驱动

回答面试官，我为什么不用context而用redux

## 57.优化：react组件调试工具

React Developer Tools

1.方便寻找组件

2.方便查看组件状态

3.性能调试

## 58.优化：浏览器性能检测与调优

![image-20250113165350948](http://139.196.79.103:9001/myimages/imgs/20250113165350999.png)

![image-20250113165404912](http://139.196.79.103:9001/myimages/imgs/20250113165404964.png)

谷歌浏览器在Performance面板

## 59.难点亮点：浏览器核心性能指标优化

https://www.yuque.com/cuggz/feplus/zlgytg2b2b7crkqc#grJv9

## 60. 难点亮点：利用灯塔查看性能指标与建议

![image-20250113172532366](http://139.196.79.103:9001/myimages/imgs/20250113172532416.png)

使用Chrome的Lighthouse

![image-20250113172502811](http://139.196.79.103:9001/myimages/imgs/20250113172502863.png)

## 61.难点亮点：利用开发环境缓存，提升本地编译速度

数据：从30s到3s

大概5-10倍的性能提示

利用webpack5的cache机制，在node_modules目录下有个.cache目录

Webpack vite能保持最新版本是最佳

## 62.优化：git commit优化

根据自己的业务情况进行调整，对git commit分类

查阅：git flow工作流

```javascript
一般的git提交
git add .
git commit -m '你的这段代码写了什么'

大厂提交用的比较多的
git add .
git commit -m 'feat: 业务开发'
git commit -m 'fix: 解决了什么bug'
git commit -m 'chore: 修改跟工程化配置相关的'
```

## 63.优化：git的版本管理与tag

项目重大版本打tag

重大版本更新（有非常大的api变化）、小范围调整api、优化或者修改bug

vue1.0.0

vue2.0.0

vue2.1.0

vue2.1.1

项目写完，打一个tag

一般业务项目，最多打到第二个层级

通常打第一个大版本也够用

看业务体量，项目越庞大，版本号越细

## 64.优化：git的分支管理优化

面试官爱问，研发流程

一般

master（线上，线上永远只运行master）

test（测试环境，部署给测试进行测试）

dev（开发环境）

人多的情况下，dev分支会比较多

拉取master分支最新代码，新建分支，格式如下：

每个公司不一样，具体看自己公司的

```javascript
20241010-cml-addOrder
```

时间-人名-功能

如果是修复bug，格式一般如下：

多个fix

```javascript
20241010-cml-fixAddOrder
```

## 用户交互优化

## 65.难点亮点：单页面应用程序的数据闪烁问题

在tab切换时，如果增加了2条数据就会有个数据跳跃

3条=>loading=>3条

3条=>loading=>5条

有一个数据跳跃。

解决：组件销毁的时候，把数据清空。

## 66.优化：loading状态优化处理

* 小于500ms的loading，让它不显示
* loading状态的数据建议放在redux或pinia中，因为可能loading状态还和某一个按钮能否点击相关
* 所有需要异步拉数据，都建议放loading

## 67.业务界面会在登录页闪烁跳出

很多项目有这个问题，几乎没有优化

比如后台管理系统访问一个路由，在还没有登录的情况下，就会重定向到登录页，但是这个时候就会闪烁一下。

出现顺序

* 先业务页面
* 跳到登录

判断怎么写？

默认是隐藏，然后再显示。如果反过来就会闪烁。

解决：

在layout中加个判断，默认没有用户信息，所以隐藏

![image-20250113181154695](http://139.196.79.103:9001/myimages/imgs/20250113181154759.png)

## 渲染优化

## 68.优化：渲染优化的原则是什么？

指导思想：解决主要矛盾。不是任何问题，都要优化。

架构师：提方针，提方案，提思想。

![image-20250113181857308](http://139.196.79.103:9001/myimages/imgs/20250113181857350.png)

我是每个组件都要去尽显渲染监测吗？

核心主要组件要监测。

看项目，有没有复杂的组件。

原则：一般项目都遵循28定律，**20%以内的组件比较复杂**，剩下的都简单。

## 69.优化：组件数据驱动渲染的设计原则

尽量避免顶层组件频繁的render。

我们在组件的store中读取数据的时候，尽量只用这个组件需要的数据，不要读取子组件或者其他组件需要的数据。

通常通过props传递数据给子组件，不要超过3层，尽量自己取自己的数据用，不要帮别人取。

超过3层：

* 可能有性能问题
* 维护不方便

## 70.优化：组件遍历的时候要加key

key一定不能是index，不能是随机数。

key一般是后台生成的。

ai只能辅助我们开发，但是绝对不能完全相信它。

key其实就是diff对比的标识，可以提升性能。

![image-20250113191813800](http://139.196.79.103:9001/myimages/imgs/20250113191813851.png)

## 71.优化：React中的useMemo对渲染的优化

useMemo和vue的计算属性比较像

组件ExpensiveCalculation接受props number，我们对number取做memo，缓存基于number的计算结果。

6=>6的阶乘的结果

5=>5的阶乘的计算结果

6=>这个结果是重新计算还是利用缓存？

重新计算的。

memo的核心是，我的依赖一直没有发生变化，但是我不想重新计算。

![image-20250113192440105](http://139.196.79.103:9001/myimages/imgs/20250113192440165.png)

## 72.优化：React中的useCallback带来的优化

useCallback的核心作用是保持函数的引用。

可以结合React.memo，在父组件渲染的时候，让子组件不要render。

也是一种减少render的手段。

![image-20250113192957502](http://139.196.79.103:9001/myimages/imgs/20250113192957560.png)

![image-20250113202616057](http://139.196.79.103:9001/myimages/imgs/20250113202616114.png)

## 73.难点亮点：关于useMemo和useCallback在项目中的技术选型

我在什么时候需要用useMemo和useCallback？

反面教材：见到组件就用。

* 开发麻烦
* 维护困难
* 增加心智负担
* 性能反而下降，增大内存开销

正确的方式：

解决主要矛盾，在很复杂的组件，可能会出现性能问题的地方用。

## 74.难点亮点：React开启并发模式

react默认是同步渲染。

并发是需要开启的。

**ReactDOM.createRoot一定要用这个方式渲染，才能拥有并发能力。**

而不是ReactDOM.render

而且此时依然是同步，想并发还需要进一步操作。

![image-20250113204446304](http://139.196.79.103:9001/myimages/imgs/20250113204446352.png)

## 75.难点亮点：使用startTransition开启并发更新

react的并发不是说开启了就是全量。

而是，我影响哪一段数据，跟这个数据相关的ui就是并发渲染的。

![image-20250113204601040](http://139.196.79.103:9001/myimages/imgs/20250113204601098.png)

## 76.难点亮点：使用deferredValue开启并发更新

deferredValue影响了谁，谁的渲染就是并发。

![image-20250113204721785](http://139.196.79.103:9001/myimages/imgs/20250113204721842.png)

## 77.难点亮点：并发更新真的让性能变好了吗？

其实性能没有变好。

核心：让体验更好。

一段任务js需要执行3000ms。

我把这个任务拆分了，执行10ms歇息一下，再接着执行，执行300次。

消耗的CPU的计算量是没有变化的，甚至可能更多。

任务总量3000ms+任务拆分体系维护（并发运行的机制）

用户的感受是变快了，用户会感觉性能还不错。

react其实是用了一个障眼法。

## 78.难点亮点：vue可以并发更新吗？

实际上vue是不需要并发更新。

这个问题，尤大大解答过。

react每次更新是自顶向下。

vue是依赖收集，精准更新。

vue=精准更新，谁变就更新谁+非并发更新（渲染是异步）

vue的组件处理过程，js是同步，但是UI的渲染是异步的。

vue的渲染最终会在微任务中执行。nextTick

AI会代替前端吗？

AI，比如自动驾驶出租车

便宜+技术含量低（机器识别，智障）

滴滴

贵+技术含量高（人脑）

总结：自定义越强的东西，给人操作空间越大的东西，越值钱。

AI会代替前端的机械工作。必须要人工介入的，取代不了。高级、资深肯定取代不了。

## 79.难点亮点：项目中使用并发更新的选型

关于startTransition和deferredValue，二选一。

startTransition的关注点在于组件内部的状态。

deferredValue的关注点在于外部传入组件的props。

我是组件的开发者

* 我写组件库
* 我提供组件给别人用

我希望这个组件能保证一个基本的性能还不错

使用startTransition

我是组件的使用者，我用别人的组件，使用defferedValue

## 80.难点亮点：浏览器常见内存调试手段

核心思维：解决主要矛盾

获取快照=coredump

Chrome在Memory面板，反复前进后退，看看内存是不是升上去下不来。

## 81.难点亮点：常见项目内存优化手段

在js中，内存优化可以通过以下几种手段实现：

1.避免全局变量：全局变量会一直存在于内存中，尽量使用局部变量或模块化的方式来减少全部作用域的使用。

2.使用let和const：相比于var，let和const具有块级作用域，能够更好地控制变量的生命周期，减少内存泄漏的风险

3.及时释放不再使用的对象（组件销毁）：确保不再使用的对象能够被垃圾回收机制回收。可以通过将引用设为null来帮助垃圾回收

4.使用弱引用：使用WeakMap和WeakSet来存储对象的引用，这样可以避免内存泄漏，因为它们不会阻止对象被垃圾回收

5.避免闭包造成的内存泄漏：在使用闭包时，确保不保留不必要的外部变量引用，避免造成内存泄漏

6.优化数据结构：选择合适的数据结构来存储数据，避免使用过于复杂或冗余的数据结构（时间换空间）

7.定期清理不必要的数据（谨慎）：在应用程序中定期清理不再需要的数据，尤其是在大型应用中，避免内存占用过高

通过以上手段，可以有效地优化js的内存使用，提高应用程序的性能。

## 82.优化：登录拦截的交互优化

1.没有登录

2.访问baidu.com/user

* User这个页面会发起获取用户信息的请求，后台返回401
* axios拦截器，找到后台返回的401

3.直接跳转到登录页baidu.com/login

假如我们访问的页面baid.com/ad，没有发起任务axios请求，但是这个页面我也希望做登录校验怎么办？

**路由守卫**

跳转前，拉接口，主动做登录校验。baidu.com/api/login/check

